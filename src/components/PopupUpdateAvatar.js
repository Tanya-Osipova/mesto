import PopupWithForm from "./PopupWithForm.js";


export default class PopupUpdateAvatar extends PopupWithForm {
  constructor(popupSelector, submitForm) {
    super(popupSelector);
    super(submitForm);
  }
  
  
}